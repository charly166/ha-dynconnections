{
  "config": {
    "step": {
      "user": {
        "title": "HA DynConnections",
        "description": "Enable HA DynConnections. Departure device and destination stop are configured per card, in the card's own editor when you add it to a dashboard."
      }
    },
    "abort": {
      "already_configured": "HA DynConnections is already configured."
    }
  }
}
